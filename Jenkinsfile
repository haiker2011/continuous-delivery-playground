pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sleep(time: 100, unit: 'MILLISECONDS')
      }
    }
    stage('final') {
      steps {
        echo '"finsh"'
      }
    }
  }
}